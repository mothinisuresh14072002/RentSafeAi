'use client';
import { useEffect, useState } from 'react';
import { apiClient } from '@/lib/api-client';
import { Card, CardBody, CardHeader } from '@/components/ui/Card';

export default function PaymentOperationsPage() { const [orders, setOrders] = useState<any[]>([]); const [timeline, setTimeline] = useState<any>(); useEffect(() => { apiClient('/payments/admin').then(setOrders).catch(() => setOrders([])); }, []); async function open(id: string) { setTimeline(await apiClient(`/payments/admin/${id}/timeline`)); } async function refund(id: string) { await apiClient(`/payments/${id}/refund`, { method: 'POST', body: JSON.stringify({ reason: 'Sandbox refund simulation' }) }); setOrders(await apiClient('/payments/admin')); } return <Card><CardHeader><h1 className="text-xl font-semibold">Payment operations · TEST/SANDBOX</h1></CardHeader><CardBody><div className="space-y-3">{orders.map(order => <button key={order.id} onClick={() => open(order.id)} className="block w-full text-left border rounded p-3"><div>{order.status} · {order.amount} paise</div><div className="text-sm text-slate-500">{order.paymentHolds?.length ? 'Payment hold attached' : 'No payment hold'} · {order.id}</div></button>)}</div>{timeline && <div className="mt-6 border-t pt-4"><h2 className="font-semibold">Transaction timeline</h2>{timeline.transactions?.map((event: any) => <div key={event.id} className="text-sm py-1">{event.eventType} · {event.status}</div>)}{timeline.status === 'CAPTURED' && <button onClick={() => refund(timeline.id)} className="mt-3 rounded bg-slate-900 text-white px-3 py-2">Simulate refund</button>}</div>}</CardBody></Card>; }
